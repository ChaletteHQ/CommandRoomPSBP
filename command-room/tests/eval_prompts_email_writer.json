{
  "skill": "email-writer",
  "schema_version": "1.0",
  "notes": "SPEC A8 — LLM-judgment layer for email-writer (routing + behavior). Gate 15 executor/grader; run_all.py ignores this file.",
  "prompts": [
    {"name": "route_draft_email", "kind": "positive_routing", "input": "draft an email to Rio about the sourcing spec", "assert": ["Fires email-writer"]},
    {"name": "route_reply", "kind": "positive_routing", "input": "reply to the Northstar thread", "assert": ["Fires email-writer"]},
    {"name": "negative_followups", "kind": "negative_routing", "input": "draft follow-ups from the meeting", "assert": ["Does NOT fire email-writer — routes to follow-up-ritual"]},
    {"name": "negative_triage", "kind": "negative_routing", "input": "triage my inbox", "assert": ["Does NOT fire email-writer — routes to inbox-triage"]},
    {"name": "behavior_gmail_draft", "kind": "behavior", "input": "email Rio about pricing", "assert": ["Produces a Gmail Draft, not a .docx or .md file (CONTRACT Rule 27)"]},
    {"name": "behavior_voice", "kind": "behavior", "input": "draft a cold email to a prospect", "assert": ["Runs the voice-calibration protocol with the skill's Voice Block; no banned phrases"]},
    {"name": "behavior_refs", "kind": "behavior", "input": "email Rio about the sourcing spec", "assert": ["Logs email_drafted with commitment_refs/decision_refs derived for the recipient"]}
  ]
}
