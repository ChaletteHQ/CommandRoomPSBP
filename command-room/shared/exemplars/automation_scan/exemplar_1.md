<!-- exemplar-skeleton (SPEC OUT8): structure only. Nothing in this file is
     content — no name, number, or claim below may appear in a deliverable. -->
<!-- tokens: Acme Co | Sam Sample | 90 minutes a week -->

# Automation Scan — structural exemplar (kind: automation_scan)

> STRUCTURE CONTRACT — annotations are the exemplar; sample lines are
> synthetic. Layout only (contract > exemplar > default).
>
> - Header: VERDICT-only lead (eyebrow-excluded). Verdict = the top
>   opportunity + its payback, one sentence.
> - This is a RANKED REPORT: tile band first, scored rows second, actions
>   last. No synthesis lead.
> - Visuals: tile summary band (opportunities count · time back per week ·
>   quick-wins count — drop-empty; derived from the SAME computation as the
>   ranked list, never a second pass); the ranked list uses the table
>   primitive.
> - Row grammar: rank (#N) · name (what it automates) · quantify tag (score +
>   time saved, shown so the reader can challenge the inputs) · why-now
>   ("where I saw it") · action ("scaffold #N").
> - Bucket order: Quick wins → Bigger builds → Long-term (long-term renders
>   only when asked for).
> - Ask close: "What's next" is the closing action block; on a widget turn
>   the widget IS the ask.

---

**The weekly status roundup is the top opportunity — about 90 minutes a week back for a small build.** [verdict — top opportunity + payback]

## [tile band]
[2–3 tiles, drop-empty: opportunities found · time back per week · quick wins]

## Quick wins (do these first)
[scored rows, best score first; each row:
 #1 — [what it automates] — [score + time saved] — [where I saw it: one line of real evidence, e.g. "the Acme Co thread every Friday"] — [action: scaffold #1]]

## Bigger builds, bigger payoff
[same row grammar; the 3–10 score band — worth doing, not this afternoon]

## Long-term — these change how you work
[rendered only when the requested horizon includes it; same row grammar]

## What's next
[the closing action block: which item to scaffold first (owner: Sam Sample style) — on the widget surface this section yields to the widget]
